type Query {
  webtoons(input: Input): [Webtoon]
  webtoon(id: ID!): Webtoon!
  authors: [Author!]
  genres: [Genre!]
}

type WebtoonResult {
  webtoons: [Webtoon]
  totalCounts: Int
}

input Input {
  pagination: Pagination
  filter: Filter
}

input Pagination {
  first: Int
  take: Int
}

input Filter {
  title: String
}

type Webtoon {
  id: ID!
  title: String!
  description: String!
  platform: Platform!
  isFinish: Boolean!
  isAdult: Boolean!
  isPay: Boolean!
  thumbnail: String!
  url: String!
  authors: [Author]
  genres: [Genre!]
}

enum Platform {
  NAVER
  DAUM
}

type Author {
  id: ID!
  name: String!
  webtoons: [Webtoon]
}

type Genre {
  id: ID!
  name: String!
}
