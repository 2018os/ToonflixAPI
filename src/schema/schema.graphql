type Query {
  webtoons(page: Page): [Webtoon]
  webtoon(id: ID!): Webtoon!
  authors: [Author!]
  genres: [Genre!]
}

type WebtoonResult {
  webtoons: [Webtoon]
  totalCounts: Int
}

input Page {
  first: Int
  offset: Int
}

type Webtoon {
  id: ID!
  title: String!
  description: String!
  platform: Platform!
  isFinish: Boolean!
  isAdult: Boolean!
  isPay: Boolean!
  thumbnail: String!
  url: String!
  authors: [Author]
  genres: [Genre!]
}

enum Platform {
  NAVER
  DAUM
}

type Author {
  id: ID!
  name: String!
  webtoons: [Webtoon]
}

type Genre {
  id: ID!
  name: String!
}
