type Query {
  webtoons(pagination: Pagination, filter: Filter): [Webtoon]
  webtoon(id: ID!): Webtoon!
  authors: [Author!]
  genres: [Genre!]
  collections: [Collection]
}

type Mutation {
  createCollection: Collection
}

type WebtoonResult {
  webtoons: [Webtoon]
  totalCounts: Int
}

input Pagination {
  first: Int
  take: Int
}

input Filter {
  title: String
}

type Webtoon {
  id: ID!
  title: String!
  description: String!
  platform: Platform!
  isFinish: Boolean!
  isAdult: Boolean!
  isPay: Boolean!
  thumbnail: String!
  url: String!
  authors: [Author]
  genres: [Genre!]
  collections: [Collection]
}

enum Platform {
  NAVER
  DAUM
}

type Author {
  id: ID!
  name: String!
  webtoons: [Webtoon]
}

type Genre {
  id: ID!
  name: String!
}

type Collection {
  title: String!
  description: String!
  webtoons: [Webtoon]
}
